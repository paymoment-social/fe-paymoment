export type ShellSection = "for-you" | "discover" | "notifications" | "messages" | "rewards" | "profile" | "bookmarks";
export type NavigationItem = { id: ShellSection; label: string; icon: string };
